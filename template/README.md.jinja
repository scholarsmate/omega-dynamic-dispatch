# {{ project_name }}

{{ description }}

## Install (dev)

```bash
python -m venv .venv
. .venv/bin/activate
python -m pip install -U pip
python -m pip install -e .
```

## Use

```bash
{{ project_slug }} --help
```

## Add a new command

Create a new file:

`src/{{ project_slug }}/plugins/<verb>.py`

Minimal example:

```python
from {{ project_slug }}.core.dispatch import command
from {{ project_slug }}.core.results import ResultObject

@command()
def hello(results: ResultObject, *, name: str = "world") -> None:
    """Say hello."""
    results.add_event("hello", message="Hello", details={"name": name})
```

```python
from {{ project_slug }}.core.dispatch import command
from {{ project_slug }}.core.results import ResultObject

@command(verb="report", summary="Show a short summary", positional=False)
def summarize(results: ResultObject, *, verbose: bool = False, limit: int = 5) -> None:
    """Demonstrates custom verb, summary, and option-only params."""

    results.add_event(
        "report",
        message="Summary complete",
        details={"verbose": verbose, "limit": limit},
    )
```
